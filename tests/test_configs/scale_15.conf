# Test config with 1.5 scale factor

# Set output scale
output * scale 1.5

# No gaps for predictable geometry
gaps inner 0
gaps outer 0

# Minimal border
default_border pixel 0

# Basic mod key (required)
set $mod Mod4