# Test config with no gaps for geometry tests
# This ensures predictable window sizes

# No gaps
gaps inner 0
gaps outer 0

# Minimal border
default_border pixel 0

# Basic mod key (required)
set $mod Mod4