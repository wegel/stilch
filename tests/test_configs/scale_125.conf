# Test config with 1.25 scale factor
# This simulates a 1080p display with fractional scaling

# Set output scale
output * scale 1.25

# No gaps for predictable geometry
gaps inner 0
gaps outer 0

# Minimal border
default_border pixel 0

# Basic mod key (required)
set $mod Mod4